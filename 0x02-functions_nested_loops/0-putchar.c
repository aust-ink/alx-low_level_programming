#include "main.h"
/**
 * _putchar - writes the character c to stdout
 *
 * Return: always 0 (success)
 */
int _putchar(void)
{
	char c[8] = "_putchar";

	int i = 0;

	while (i < 8)
	{
		_putchar(c(i));
		i++;
}
	_putchar('\n');
	return (0);
}
